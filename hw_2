#include <stdio.h>

int main(void) {
	char arr[100] = "hello";
	int count = 0,i=0;
	printf("문자열을 입력하세요:");
	scanf_s("%s", arr,sizeof(arr));

	printf("입력된 문자열은\n \"%s\" \n입니다.\n", arr);

	while (arr[i] != '\0') {
		count++;
		i++;
	}

	printf("입력된 문자열의 길이 = %d", count);

	return 0;
}
